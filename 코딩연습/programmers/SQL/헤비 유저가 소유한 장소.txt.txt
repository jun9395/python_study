SELECT A.ID, A.NAME, A.HOST_ID

FROM PLACES AS A

LEFT JOIN (SELECT HOST_ID, COUNT(HOST_ID) AS COUNT_HOST FROM PLACES GROUP BY HOST_ID) AS B

ON A.HOST_ID = B.HOST_ID

WHERE B.COUNT_HOST >= 2

ORDER BY ID;